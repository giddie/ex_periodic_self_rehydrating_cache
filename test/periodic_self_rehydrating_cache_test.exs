defmodule PeriodicSelfRehydratingCacheTest do
  use ExUnit.Case
  doctest PeriodicSelfRehydratingCache

  test "greets the world" do
    assert PeriodicSelfRehydratingCache.hello() == :world
  end
end
